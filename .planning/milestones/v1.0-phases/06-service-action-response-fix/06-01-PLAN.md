---
phase: 06-service-action-response-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/services.js
autonomous: true
requirements:
  - DASH-02

must_haves:
  truths:
    - "POST /api/services/:name/action response includes fragmentPath field"
    - "POST /api/services/:name/action response includes writable field"
    - "After any action (start/stop/restart/enable/disable), the user badge does not disappear from the service row"
  artifacts:
    - path: "server/routes/services.js"
      provides: "Action endpoint with complete service shape"
      contains: "fragmentPath"
  key_links:
    - from: "server/routes/services.js"
      to: "src/pages/Home.tsx:handleServiceUpdate"
      via: "action response service object"
      pattern: "fragmentPath.*writable"
---

<objective>
Add `fragmentPath` and `writable` fields to the POST /api/services/:name/action response so the optimistic update in handleServiceUpdate preserves the full ServiceEntry shape.

Purpose: Close INT-01 and FLOW-01 — the "user" badge disappears after actions because the response lacks these two fields, causing the optimistic replacement to overwrite them with undefined.
Output: Updated server/routes/services.js with complete service response shape.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-service-action-response-fix/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FragmentPath to SHOW_PROPS and include fragmentPath+writable in action response</name>
  <files>server/routes/services.js</files>
  <read_first>
    - server/routes/services.js (current action endpoint, SHOW_PROPS constant)
    - server/utils/systemctl.js (reference implementation of fragmentPath/writable derivation, lines 86-110)
  </read_first>
  <action>
In `server/routes/services.js`, make these changes:

1. Add `resolve` import at the top of the file:
   ```js
   import { resolve } from 'node:path'
   ```

2. Add `FragmentPath` to the `SHOW_PROPS` constant (line 8-9). Change from:
   ```
   'Id,Description,MainPID,MemoryCurrent,CPUUsageNSec,ActiveEnterTimestamp,ActiveState,LoadState,SubState,UnitFileState'
   ```
   to:
   ```
   'Id,Description,MainPID,MemoryCurrent,CPUUsageNSec,ActiveEnterTimestamp,ActiveState,LoadState,SubState,UnitFileState,FragmentPath'
   ```

3. In the POST `/:name/action` handler, inside the `service` object literal (after the `isWatched` field on line 73), add these two fields:
   ```js
   fragmentPath: detail.FragmentPath || null,
   writable: detail.FragmentPath ? resolve(detail.FragmentPath).startsWith('/etc/systemd/system/') : false,
   ```

These patterns exactly match the reference implementation in `server/utils/systemctl.js` lines 106-107. No other changes needed — the frontend `ServiceEntry` type already expects both fields.
  </action>
  <verify>
    <automated>grep -n "FragmentPath" server/routes/services.js && grep -n "fragmentPath" server/routes/services.js && grep -n "writable" server/routes/services.js && grep -n "resolve" server/routes/services.js && cd /home/sanchez/proyectos/objetiva-vps-systemdctl && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - server/routes/services.js line 1-3 area contains `import { resolve } from 'node:path'`
    - SHOW_PROPS string contains `FragmentPath` (grep returns match)
    - service object in POST handler contains `fragmentPath: detail.FragmentPath || null`
    - service object in POST handler contains `writable: detail.FragmentPath ? resolve(detail.FragmentPath).startsWith('/etc/systemd/system/') : false`
    - `npm run build` exits with code 0 (no compilation errors)
  </acceptance_criteria>
  <done>POST /api/services/:name/action response includes fragmentPath and writable fields using the same derivation logic as getAllServices(). Build passes.</done>
</task>

</tasks>

<verification>
1. `grep "fragmentPath" server/routes/services.js` returns matches in the action response object
2. `grep "writable" server/routes/services.js` returns match in the action response object
3. `grep "FragmentPath" server/routes/services.js` returns matches in both SHOW_PROPS and detail access
4. `npm run build` succeeds with exit code 0
</verification>

<success_criteria>
- The POST /api/services/:name/action response service object has exactly the same fields as the objects returned by GET /api/services
- No frontend changes needed — the existing ServiceEntry type and handleServiceUpdate function work with the complete shape
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-service-action-response-fix/06-01-SUMMARY.md`
</output>

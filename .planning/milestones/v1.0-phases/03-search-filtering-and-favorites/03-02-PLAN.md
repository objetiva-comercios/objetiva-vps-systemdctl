---
phase: 03-search-filtering-and-favorites
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/SearchFilterBar.tsx
  - src/pages/Home.tsx
  - src/components/ServiceTable.tsx
  - src/components/ServiceRow.tsx
autonomous: true
requirements:
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Typing in the search box filters the service list in real time to show only services whose names contain the typed text"
    - "Selecting a status filter (running, stopped, failed) shows only services matching that state"
    - "Clicking a star icon on a service row toggles its watched state, updating the UI immediately"
    - "Watched services appear in a dedicated section above the main table, regardless of active search or status filter"
    - "The main table shows a result count and empty state message when filters match nothing"
    - "Search and status filter can be combined (e.g., search 'nginx' + filter 'running')"
  artifacts:
    - path: "src/components/SearchFilterBar.tsx"
      provides: "Search input, status filter buttons, result count"
      min_lines: 40
    - path: "src/pages/Home.tsx"
      provides: "Filter state, useMemo derivation, watched section, toggle handler"
      contains: "useMemo"
    - path: "src/components/ServiceTable.tsx"
      provides: "onToggleWatch prop passed to ServiceRow"
      contains: "onToggleWatch"
    - path: "src/components/ServiceRow.tsx"
      provides: "Star/StarOff toggle button in actions cell"
      contains: "Star"
  key_links:
    - from: "src/pages/Home.tsx"
      to: "src/components/SearchFilterBar.tsx"
      via: "Props: searchQuery, onSearchChange, statusFilter, onStatusChange, counts"
      pattern: "SearchFilterBar"
    - from: "src/pages/Home.tsx"
      to: "/api/watched"
      via: "fetch in handleToggleWatch for optimistic toggle"
      pattern: "api/watched"
    - from: "src/components/ServiceRow.tsx"
      to: "src/pages/Home.tsx"
      via: "onToggleWatch callback passed through ServiceTable"
      pattern: "onToggleWatch"
---

<objective>
Add search bar, status filter buttons, star toggle, and watched services section to the dashboard UI.

Purpose: DASH-04 (search by name), DASH-05 (filter by status), and DASH-06 (watched section in the UI) complete the admin's ability to navigate and organize their service list efficiently.

Output: SearchFilterBar component, updated Home page with filter state and watched section, star toggle button in service rows.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-filtering-and-favorites/03-RESEARCH.md
@.planning/phases/03-search-filtering-and-favorites/03-01-SUMMARY.md
@src/pages/Home.tsx
@src/components/ServiceTable.tsx
@src/components/ServiceRow.tsx
@src/types/service.ts
@src/hooks/useServicePolling.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SearchFilterBar component and ServiceRow star toggle</name>
  <files>src/components/SearchFilterBar.tsx, src/components/ServiceRow.tsx, src/components/ServiceTable.tsx</files>
  <action>
1. Create `src/components/SearchFilterBar.tsx`:
   - Import `Search` and `X` icons from lucide-react
   - Define `StatusFilter` type: `'all' | 'running' | 'stopped' | 'failed'`
   - Props interface: `searchQuery: string`, `onSearchChange: (q: string) => void`, `statusFilter: StatusFilter`, `onStatusChange: (s: StatusFilter) => void`, `totalCount: number`, `filteredCount: number`
   - Render a flex row with:
     a. Search input: relative container with Search icon left-positioned, text input with `pl-7 pr-7 py-1.5`, X clear button shown when searchQuery is non-empty. Use existing design tokens: `bg-bg-elevated border border-border text-text-primary placeholder:text-text-muted focus:border-accent`. Font: `text-xs font-mono`. Max width: `max-w-xs flex-1`.
     b. Status filter button group: `STATUS_BUTTONS` array with `{ label: 'All', value: 'all' }, { label: 'Running', value: 'running' }, { label: 'Stopped', value: 'stopped' }, { label: 'Failed', value: 'failed' }`. Add counts in parentheses — pass counts via a `statusCounts` prop: `{ all: number, running: number, stopped: number, failed: number }`. Active button: `bg-accent text-bg-base`. Inactive: `text-text-muted hover:text-accent border border-border hover:border-accent`. Size: `px-2 py-1 rounded text-xs font-mono`.
     c. Result count: shown only when filters are active (searchQuery or statusFilter !== 'all'). Format: `"{filteredCount} of {totalCount}"`. Style: `text-text-muted text-xs font-mono`.
   - Export as default

2. Modify `src/components/ServiceRow.tsx`:
   - Add `Star` and `StarOff` to the lucide-react imports
   - Add `onToggleWatch: (unit: string, currentlyWatched: boolean) => void` to `ServiceRowProps`
   - In the actions `<td>`, add a star toggle button BEFORE the existing action buttons (so it appears at the left of the actions cell):
     ```
     <button
       onClick={() => onToggleWatch(service.unit, service.isWatched)}
       title={service.isWatched ? 'Unwatch' : 'Watch'}
       className={`p-1 rounded transition-colors ${
         service.isWatched
           ? 'text-accent hover:text-accent/70'
           : 'text-text-muted hover:text-accent'
       }`}
     >
       {service.isWatched
         ? <Star className="w-3.5 h-3.5 fill-current" />
         : <StarOff className="w-3.5 h-3.5" />
       }
     </button>
     ```
     Note: `fill-current` on Star makes the watched star appear filled/solid, matching conventional star-toggle UX.

3. Modify `src/components/ServiceTable.tsx`:
   - Add `onToggleWatch: (unit: string, currentlyWatched: boolean) => void` to `ServiceTableProps`
   - Also add optional `isWatchedSection?: boolean` prop (used for visual distinction of the watched table)
   - Pass `onToggleWatch` to each `ServiceRow` component
   - Update the empty state message: if `isWatchedSection` is not set, show "No services match your filters" instead of "No services found" (per research Pitfall 6)
  </action>
  <verify>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- SearchFilterBar.tsx exists and exports a default component
  </verify>
  <done>SearchFilterBar component renders search input with clear button and status filter buttons with counts. ServiceRow has a star toggle button. ServiceTable passes onToggleWatch through to rows. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Home page filter state, useMemo derivation, watched section, and optimistic toggle</name>
  <files>src/pages/Home.tsx</files>
  <action>
Modify `src/pages/Home.tsx` to integrate all Phase 3 features:

1. **New imports:**
   - `useMemo` from react (add to existing import)
   - `Star` from lucide-react (add to existing import)
   - `SearchFilterBar` from '../components/SearchFilterBar'
   - Export `StatusFilter` type from SearchFilterBar or define locally: `type StatusFilter = 'all' | 'running' | 'stopped' | 'failed'`

2. **New state:**
   - `const [searchQuery, setSearchQuery] = useState('')`
   - `const [statusFilter, setStatusFilter] = useState<StatusFilter>('all')`

3. **Derived data via useMemo:**
   a. `filteredServices` — derives from `services`, `searchQuery`, `statusFilter`:
      - If searchQuery is non-empty (trimmed), filter by `s.unit.toLowerCase().includes(searchQuery.toLowerCase())`
      - CRITICAL: "running" filter uses `s.sub === 'running'` (NOT `s.active === 'active'`) to exclude exited one-shot services. Per research Pitfall 1.
      - "stopped" filter uses `s.active === 'inactive'`
      - "failed" filter uses `s.active === 'failed'`
      - Dependencies: `[services, searchQuery, statusFilter]`

   b. `watchedServices` — derives from `services` (unfiltered, NOT from filteredServices. Per research Pitfall 2):
      - `services.filter(s => s.isWatched)`
      - Dependencies: `[services]`

   c. `statusCounts` — derives from `services` (unfiltered) via a single `reduce()`:
      ```typescript
      const statusCounts = useMemo(() => {
        return services.reduce(
          (acc, s) => {
            acc.all++
            if (s.sub === 'running') acc.running++
            else if (s.active === 'inactive') acc.stopped++
            else if (s.active === 'failed') acc.failed++
            return acc
          },
          { all: 0, running: 0, stopped: 0, failed: 0 }
        )
      }, [services])
      ```

4. **handleToggleWatch function:**
   - Optimistic UI update: immediately toggle `isWatched` on the matching service via `setServices(prev => prev.map(...))`
   - Fire fetch to `/api/watched/${encodeURIComponent(unit)}` with method POST (if not watched) or DELETE (if watched)
   - On fetch failure, revert the optimistic update
   - Use try/catch pattern from research Pattern 6

5. **Updated JSX layout:**
   a. After SystemHeader, render SearchFilterBar with all props:
      - `searchQuery`, `onSearchChange: setSearchQuery`
      - `statusFilter`, `onStatusChange: setStatusFilter`
      - `statusCounts`
      - `totalCount: services.length`, `filteredCount: filteredServices.length`

   b. Watched section (render BEFORE the main table, only when `watchedServices.length > 0`):
      - Header row with Star icon and "Watched ({count})" label, styled: `text-text-muted text-xs font-mono uppercase tracking-wider`
      - A ServiceTable wrapping the watched services with `border-accent/20` border for visual distinction
      - Pass `isWatchedSection` prop to ServiceTable
      - Pass `onToggleWatch={handleToggleWatch}` and `onServiceUpdate={handleServiceUpdate}`

   c. Main service table:
      - Pass `filteredServices` instead of `services`
      - Pass `onToggleWatch={handleToggleWatch}`
      - Update the status bar count to show `filteredServices.length` when filters are active, `services.length` otherwise

   d. Update the status bar "X services" text:
      - When filters are active, show: `{filteredServices.length} of {services.length} services`
      - When no filters: `{services.length} services`
  </action>
  <verify>
- `npm run build` succeeds
- Open the app in browser:
  1. Search bar appears above the service table
  2. Typing "nginx" filters the list to show only nginx-related services
  3. Clicking "Running" filter shows only running services (should be ~24, not ~60)
  4. Clicking the star on a service makes it appear in the watched section above
  5. Refreshing the page preserves the watched state (star still filled, watched section still shows)
  6. Searching while services are watched does NOT hide the watched section
  7. Clearing search (X button) restores full list
  8. Status count badges show correct numbers
  </verify>
  <done>Home page has search input that filters by name in real time. Status filter buttons correctly distinguish running from exited services. Star toggle uses optimistic updates with server persistence. Watched section renders above the main table with all watched services regardless of active filters. Empty filter state shows descriptive message.</done>
</task>

</tasks>

<verification>
1. Search: typing "ssh" shows only ssh-related services; clearing shows all
2. Filter: "Running" shows ~24 services (not ~60 — exited services excluded); "Stopped" shows ~113; "Failed" shows matching count
3. Combined: search "docker" + filter "Running" shows only running docker services
4. Watch: click star on any service — it appears in watched section above; star appears filled
5. Persistence: refresh page — watched section still shows same services
6. Watch independence: search for unrelated term — watched section still visible at top
7. Empty state: search for "zzzznonexistent" — shows "No services match your filters"
8. Counts: filter buttons show accurate counts; "X of Y" shown when filtering
9. npm run build succeeds with no errors
</verification>

<success_criteria>
- Search box filters services by name in real time (case-insensitive substring match)
- Status filter buttons correctly categorize: running (sub === 'running'), stopped (active === 'inactive'), failed (active === 'failed')
- Star toggle updates UI instantly (optimistic) and persists to server
- Watched section renders above main table, unaffected by search/filter state
- Status count badges reflect accurate system state
- npm run build and npx tsc --noEmit both pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-search-filtering-and-favorites/03-02-SUMMARY.md`
</output>
